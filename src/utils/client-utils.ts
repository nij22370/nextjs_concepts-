import "client-only";
const clientSideFunction = () => {
  console.log(`use window object,use localSrorage`);
  return "client  result  ";
};

export default clientSideFunction;
// client only code
